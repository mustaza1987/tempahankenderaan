import 'package:flutter/material.dart';

class PengurusanPage extends StatelessWidget {
  const PengurusanPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Pengurusan')),
      body: const Center(child: Text('Selamat datang, Pengurusan')),
    );
  }
}
