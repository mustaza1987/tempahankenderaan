import 'package:flutter/material.dart';

class PentadbirPage extends StatelessWidget {
  const PentadbirPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Pentadbir')),
      body: const Center(child: Text('Selamat datang, Pentadbir')),
    );
  }
}
